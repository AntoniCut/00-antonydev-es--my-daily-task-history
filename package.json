{
  "name": "my-daily-task-history",
  "version": "1.0.0",
  "private": true,
  "description": "Diario personal de actividades diarias con tareas",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"pnpm --filter server run dev\" \"pnpm --filter client run dev\"",
    "dev:server": "pnpm --filter server run dev",
    "dev:client": "pnpm --filter client run dev",
    "build": "pnpm --filter server run build && pnpm --filter client run build",
    "preview": "pnpm --filter server run start",
    "start": "pnpm --filter server run start",
    "create-user": "pnpm --filter server run create-user"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}